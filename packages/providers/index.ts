// UI-safe exports (no native binaries, safe to bundle in renderer)
export * from './providers'
export * from './config'
export * from './colors'

// Factory is main-process only — import from '@openframe/providers/factory'
