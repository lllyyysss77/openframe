{
  "name": "openframe-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter openframe dev",
    "build": "pnpm --filter openframe build",
    "lint": "pnpm -r lint",
    "db:generate": "pnpm --filter openframe db:generate",
    "db:migrate": "pnpm --filter openframe db:migrate"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "electron"]
  }
}
