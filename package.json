{
  "name": "openframe-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter @openframe/desktop dev",
    "build": "pnpm --filter @openframe/desktop build",
    "lint": "pnpm -r lint",
    "db:generate": "pnpm --filter @openframe/desktop db:generate",
    "db:migrate": "pnpm --filter @openframe/desktop db:migrate"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "electron"]
  }
}
