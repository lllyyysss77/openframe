{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --frozen-lockfile --ignore-scripts --filter openframe-web... --filter openframe-ui...",
  "buildCommand": "if [ -f apps/web/package.json ]; then WEB_DIR=apps/web; elif [ -f ../web/package.json ]; then WEB_DIR=../web; else echo 'Cannot locate apps/web from current working directory' && exit 1; fi && pnpm -C \"$WEB_DIR\" build && rm -rf dist && mkdir -p dist && cp -R \"$WEB_DIR\"/dist/. dist/",
  "devCommand": "pnpm --filter openframe-web dev",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/apps/web/api/:path*"
    }
  ],
  "functions": {
    "apps/web/api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  }
}
